---
/**
 * 首页 - 重定向到今日日期
 * 
 * 使用 Asia/Shanghai 时区确保国内用户访问到正确的“今日”日期。
 */

// 获取今天的上海日期 (YYYY-MM-DD)
const today = new Intl.DateTimeFormat('en-CA', {
    timeZone: 'Asia/Shanghai',
    year: 'numeric',
    month: '2-digit',
    day: '2-digit'
}).format(new Date());

return Astro.redirect(`/day/${today}`);
---

